[project]
name = "marimo-pacmap-animation"
version = "0.0.0"
requires-python = "==3.10.*"
dependencies = [
    "numpy==2.2.6",
    "pacmap==0.9.1",
    "matplotlib==3.10.9",
    "scikit-learn==1.7.2",
]

[dependency-groups]
dev = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["tests"]
